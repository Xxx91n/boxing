Boxing 2026.9.15

一次稳定性收口：跨标签星标同步修复、暗色对比度达标、WebDAV 同步升级为三路合并。

## 这版你能感觉到的变化

### 修复
- **跨标签星标同步** — 在一个窗口给大盒子加星，新开的标签页里也能看到
- **暗色空态按钮** — 空画布/空盒子里的「添加书签」按钮在暗色模式下对比度达标（文本 7.73:1、边框 4.45:1），看得见也点得到
- **收起后再进入自动展开** — 大盒子悬停收起后重新进入，内容自动展开恢复正常
- **WebDAV 私网开关全链路生效** — `.local` 等内网/本机地址在网址校验与导出路径统一按你的 opt-in 设置执行

### 改进
- **WebDAV 同步升级为三路合并** — 两台设备在同一大盒子下各自新增的小盒子，拉取后双方都保留；同 id 分叉自动生成「冲突副本」，可在 设置 → 数据区 查看与导出，不再静默吞掉一侧

### 内部
- 首开零闪现纳入 e2e 回归；测试用例真实凭据全面清扫为环境占位
- CI 主 lane 全绿（含 data-golden）；残红台账仅剩具名 flaky

## 从哪里安装（推荐）

| 浏览器 | 安装 |
|---|---|
| **Firefox** | [Firefox Browser ADD-ONS](https://addons.mozilla.org/zh-CN/firefox/addon/boxing-newtab/) |
| **Edge / Chromium** | [Edge Add-ons](https://microsoftedge.microsoft.com/addons/detail/inkgieheaiifkkdmlpggihjplkkgpepi) |

> 本 Release **不再提供** \.xpi\ / \.crx\ 下载。签名与更新由商店负责。

## 开发者 / 源码

- 源码见本仓库；本地构建：`npm ci && npm run build`
- 附件含商店源码审核包与 Chromium/Firefox zip（侧载/调试用）；校验见 `SHA256SUMS.txt`
- 隐私政策：https://xxx91n.github.io/boxing/privacy-policy.html

### 内部
- 发行 zip 不再打包 `.scratch` 工作日志（打包卫生）。

## 已知说明

- 本版为 **2026.9.15**。数据安全相关门禁背景见仓库 ADR-0017；发布检查单在文档区。

---

# Boxing 2026.9.15

A stability closeout: cross-tab star-sync fix, dark-mode contrast to WCAG targets, and WebDAV layout merge upgraded to three-way.

## What you will notice

### Fixed
- Cross-tab star / parent sync no longer flakes in multi-window use.
- Dark empty-state “add bookmark” controls meet contrast targets (text and border).
- WebDAV concurrent edits merge by box id — both sides keep their new small boxes; conflicts become browsable copies instead of silent loss.
- Boot theme: if init fails early, the temporary content mask always clears (no permanent blank page).

### Improved
- Search input debounce (fewer full rebuilds while typing).
- Settings: WebDAV private-host opt-in documented; credentials are per-install.
- Version strings all read the same CalVer source (2026.9.15).

### Internal
- New CI guards: calver surface, onboarding dismiss, locale README claims, contrast pairs, docs links, waiver revocation criteria.
- Release zip no longer ships `.scratch` working notes.

## Install
Prefer official store listings once 2026.9.15 is published. GitHub Releases track the same notes for local testing.

## Known notes
- Store listings may still show 2026.9.12 until review completes.
- Release gates: G-A (CI) + G-B (user golden path) + G-C (Pages). See `docs/release-status.md`.
