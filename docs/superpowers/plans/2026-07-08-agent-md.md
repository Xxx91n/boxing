# Agent.md Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create two machine-readable `agent.md` files: a root browser-extension development guide and a Qlearly-specific guide that includes the root guide plus project specialization.

**Architecture:** The root `agent.md` contains only stable CRX/browser-extension operating rules, approved CRX tool recipes, verification commands, and UI/aesthetic workflow rules. The Qlearly `agent.md` copies the root guide verbatim, then appends Qlearly-specific project facts, CSS-token aesthetic constraints, safe modification rules, and validation rules.

**Tech Stack:** Markdown, Windows paths, opencode, skm, WXT, Chrome DevTools MCP, Modern Web Guidance skills, frontend-design skill.

## Global Constraints

- Work root: `D:\Aworker\crx`.
- Create `D:\Aworker\crx\agent.md`.
- Create `D:\Aworker\crx\qlearly_bookmark-0.0.2\agent.md`.
- Do not edit global opencode, Claude Code, Cursor, or skm configuration.
- Do not add npm dependencies.
- Do not include full skm 27-skill inventory.
- Do not include full opencode 6-MCP inventory.
- Do not include WXT-vs-Plasmo-vs-CRXJS historical comparison.
- Do not include non-CRX tools: docx, pdf, pptx, aihot, khazix, maya, omniroute, exa, perplexity, codegraph.
- Root guide may include only CRX-relevant tools: `chrome-extensions`, `modern-web-guidance`, `frontend-design`, `chrome-devtools-mcp`, optional `playwright-mcp`, and WXT.
- Qlearly guide must contain the full root guide content plus Qlearly-specific constraints.
- Qlearly plan/status tables remain in `docs/superpowers/specs/2026-07-08-agents-md-design.md`, not in either `agent.md`.

---

## File Structure

| File | Action | Responsibility |
|---|---|---|
| `agent.md` | Create | Root browser-extension agent guide, machine-readable and project-local. |
| `qlearly_bookmark-0.0.2/agent.md` | Create | Full copy of root guide plus Qlearly-specific instructions. |
| `docs/superpowers/plans/2026-07-08-agent-md.md` | Create | This implementation plan. |

---

### Task 1: Create Root Browser Extension Agent Guide

**Files:**
- Create: `D:\Aworker\crx\agent.md`

**Interfaces:**
- Consumes: `docs/superpowers/specs/2026-07-08-agents-md-design.md`
- Produces: Root guide content that Task 2 must copy verbatim into `qlearly_bookmark-0.0.2/agent.md`.

- [ ] **Step 1: Write `agent.md` with exact content**

Create `D:\Aworker\crx\agent.md` with this complete Markdown:

```markdown
# Browser Extension Agent Guide

## Applicability

| Field | Value |
|---|---|
| Scope | Browser extension development under `D:\Aworker\crx` |
| Primary reader | Future AI coding agents |
| Style | Machine-readable first: tables, command blocks, MUST/MUST NOT rules |
| Target browsers | Chrome + Firefox unless a project-specific guide narrows scope |
| Default implementation style | Minimal dependencies, standards-first WebExtension code |

## Hard Rules

| Rule ID | Type | Rule |
|---|---|---|
| CRX-R-001 | MUST | Treat this file as project-local operating instructions for browser extension work only. |
| CRX-R-002 | MUST | Prefer Manifest V3 for new or migrated Chrome/Firefox extension work. |
| CRX-R-003 | MUST | Preserve browser-extension constraints: popup, options page, new tab page, content scripts, background service worker, permissions, host permissions, and store submission metadata. |
| CRX-R-004 | MUST | Use `chrome-extensions` and `modern-web-guidance` skills before changing extension APIs, Manifest V3 behavior, service workers, content scripts, popup UI, side panel UI, or Chrome Web Store metadata. |
| CRX-R-005 | MUST | Use `frontend-design` before UI aesthetic changes to popup, side panel, options page, new tab page, or injected content script UI. |
| CRX-R-006 | MUST | Use Chrome DevTools MCP for browser/runtime validation when available. |
| CRX-R-007 | MUST | Keep Chrome + Firefox compatibility visible in design and validation steps. |
| CRX-R-008 | MUST NOT | Do not add npm dependencies unless the user explicitly approves. |
| CRX-R-009 | MUST NOT | Do not default to shadcn/ui, Tailwind, Figma MCP, React, Plasmo, Browserbase, or Stagehand. |
| CRX-R-010 | MUST NOT | Do not include non-CRX tools in extension implementation reasoning unless the user explicitly asks. |
| CRX-R-011 | MUST NOT | Do not migrate an existing unpacked extension into WXT unless the user explicitly requests a full framework migration. |
| CRX-R-012 | IF-THEN | If a project-specific `agent.md` exists in a subdirectory, follow it after this guide. |

## Approved CRX Toolchain

| Tool | Type | Status | Use when |
|---|---|---|---|
| `chrome-extensions` | Skill | Approved | Manifest V3, background service workers, popup/options/newtab UI, content scripts, permissions, Chrome Web Store readiness. |
| `modern-web-guidance` | Skill | Approved | Modern CSS, Baseline-compatible web APIs, browser-platform guidance. |
| `frontend-design` | Skill | Approved | High-aesthetic UI decisions for popup, side panel, options, new tab, or injected UI. |
| `chrome-devtools-mcp` | MCP | Approved | Screenshot, DOM/CSS/runtime/console/network validation in Chrome. |
| `playwright-mcp` | MCP | Optional | Firefox/cross-browser behavioral validation if configured. |
| WXT | Framework | Approved for new scaffolds | New Chrome+Firefox extension scaffolds only; do not force onto existing unpacked projects. |

## Tool Recipes

### skm skill install syntax

Use positional skill name. Do not use `--skill`.

```powershell
skm install https://github.com/GoogleChrome/modern-web-guidance-src chrome-extensions --agents-includes claude,standard
skm install https://github.com/GoogleChrome/modern-web-guidance-src modern-web-guidance --agents-includes claude,standard
```

### opencode MCP add syntax

Use command after `--`. Do not use unsupported `--command` or `--args` flags.

```powershell
opencode mcp add chrome-devtools -- npx -y chrome-devtools-mcp@latest --categoryExtensions --autoConnect
```

Required Chrome DevTools MCP flags:

| Flag | Required | Purpose |
|---|---:|---|
| `--categoryExtensions` | Yes | Enables extension-specific tools. |
| `--autoConnect` | Yes | Connects to existing Chrome profile/session where possible. |

### WXT new-project scaffold

Use WXT only for new scaffolds unless the user requests migration.

```powershell
npx -y wxt@latest init my-extension -t vanilla --pm pnpm
cd my-extension
pnpm install
```

Use `--pm`, not `--package-manager`.

### Direct WXT CLI fallback

If pnpm 11 dependency status checks fail, run WXT directly:

```powershell
node node_modules/wxt/dist/cli/index.mjs build
node node_modules/wxt/dist/cli/index.mjs build -b firefox --mv3
node node_modules/wxt/dist/cli/index.mjs zip -b firefox --mv3
```

## Build and Verification

| Scenario | Command | Expected result |
|---|---|---|
| Chrome WXT build | `node node_modules/wxt/dist/cli/index.mjs build` | `.output\chrome-mv3\manifest.json` exists. |
| Firefox WXT build | `node node_modules/wxt/dist/cli/index.mjs build -b firefox --mv3` | `.output\firefox-mv3\manifest.json` exists. |
| Firefox sources zip | `node node_modules/wxt/dist/cli/index.mjs zip -b firefox --mv3` | `.output\*-sources.zip` exists. |
| Existing unpacked extension validation | Load unpacked extension in Chrome/Firefox manually or with available MCP/browser tooling | Extension loads without manifest errors and console errors are reviewed. |
| UI verification | Use Chrome DevTools MCP screenshot/DOM/console inspection | Visual result matches requested style and console is clean. |

## UI/Aesthetic Workflow

| Step | Required action |
|---:|---|
| 1 | Use `frontend-design` for visual direction before UI edits. |
| 2 | Use `modern-web-guidance` for modern CSS/browser API constraints. |
| 3 | Prefer project-local CSS variables and native browser APIs before adding UI dependencies. |
| 4 | For content script UI, prefer Shadow DOM or isolated styles to avoid host-page style collisions. |
| 5 | Validate visual changes through Chrome DevTools MCP when available. |
| 6 | Keep extension bundle size and startup cost visible for popup/newtab surfaces. |

## Forbidden Defaults

| Default | Rule |
|---|---|
| shadcn/ui | Do not add unless user explicitly selects it. |
| Tailwind | Do not add unless user explicitly selects it. |
| Figma MCP | Do not add unless a Figma file/design workflow exists. |
| Plasmo | Do not add unless user requests Plasmo migration. |
| React/Vue rewrite | Do not add unless user approves a framework rewrite. |
| Cloud browser automation | Do not add for sensitive extension sessions unless user approves. |

## Known CLI Traps

| Trap | Correct handling |
|---|---|
| `skm install --skill` | Use `skm install [SOURCE] [SKILL_NAME]`. |
| `opencode mcp add --command/--args` | Use `opencode mcp add <name> -- <command> <args...>`. |
| `wxt init --package-manager` | Use `wxt init --pm`. |
| `ERR_PNPM_IGNORED_BUILDS` | Use direct WXT CLI fallback or approve only needed build scripts. |
| Missing extension tools in Chrome DevTools MCP | Re-add MCP with `--categoryExtensions`. |
```

- [ ] **Step 2: Verify file exists**

Run:

```powershell
Test-Path -LiteralPath "D:\Aworker\crx\agent.md"
```

Expected:

```text
True
```

- [ ] **Step 3: Verify excluded non-CRX terms are absent**

Run:

```powershell
Select-String -LiteralPath "D:\Aworker\crx\agent.md" -Pattern "docx|pdf|pptx|aihot|khazix|maya|omniroute|exa|perplexity|codegraph" -CaseSensitive:$false
```

Expected:

```text
(no output)
```

---

### Task 2: Create Qlearly Project Agent Guide

**Files:**
- Create: `D:\Aworker\crx\qlearly_bookmark-0.0.2\agent.md`

**Interfaces:**
- Consumes: Exact content from `D:\Aworker\crx\agent.md` created in Task 1.
- Produces: Qlearly-specific agent guide that includes root guide content plus specialization.

- [ ] **Step 1: Copy root guide content**

Create `D:\Aworker\crx\qlearly_bookmark-0.0.2\agent.md` with the complete content of `D:\Aworker\crx\agent.md` first.

- [ ] **Step 2: Append Qlearly specialization content**

Append this exact Markdown after the copied root guide:

```markdown

---

# Qlearly Project Specialization

## Applicability

| Field | Value |
|---|---|
| Project path | `D:\Aworker\crx\qlearly_bookmark-0.0.2` |
| Current extension | `Qlearly_bookmark` v0.0.2 |
| Current manifest | Manifest V2 |
| Target browsers | Chrome + Firefox |
| Main UI surface | New tab override: `ntp/index.html`, `ntp/ntp.css`, `ntp/ntp.js` |
| Secondary UI surfaces | `popup/`, `options/` |
| Target style | Beige, modern minimal, high whitespace, bookmark boxes/layered cards preserved |

## Project Facts

| Fact | Value |
|---|---|
| Existing new tab entry | `ntp/index.html` loads `ntp.css` and `ntp.js`. |
| Existing popup entry | `popup/popup.html`, `popup/popup.css`, `popup/popup.js`. |
| Existing options entry | `options/options.html`, `options/options.css`, `options/options.js`. |
| Existing visual asset | `ntp/ntp.css` is the main aesthetic asset and must be mined before rewriting. |
| Existing JS state | `ntp/ntp.js` contains built/minified Vue 2 runtime and renders minimal placeholder UI. |
| Existing package state | This unpacked project is not a WXT scaffold and has no WXT scripts. |
| OAuth state | Manifest includes Google Drive OAuth scope, but do not build Drive sync unless user explicitly scopes it. |

## Qlearly Hard Rules

| Rule ID | Type | Rule |
|---|---|---|
| QL-R-001 | MUST | Preserve the bookmark box/card hierarchy concept. |
| QL-R-002 | MUST | Use CSS Variables Tokens as the aesthetic dependency model. |
| QL-R-003 | MUST | Keep the visual direction beige, modern, minimal, and spacious. |
| QL-R-004 | MUST | Prefer vanilla JS + CSS tokens over framework rewrites. |
| QL-R-005 | MUST | Inspect and mine `ntp/ntp.css` before replacing styles. |
| QL-R-006 | MUST | Keep Chrome + Firefox compatibility visible during API and manifest edits. |
| QL-R-007 | MUST NOT | Do not introduce shadcn/ui, Tailwind, Figma MCP, Plasmo, React, or Vue rewrite by default. |
| QL-R-008 | MUST NOT | Do not remove the new tab override behavior unless user explicitly approves. |
| QL-R-009 | MUST NOT | Do not activate Google Drive sync/OAuth work unless user explicitly scopes it. |
| QL-R-010 | IF-THEN | If converting to MV3, map `browser_action` to `action` and `background.scripts` to `background.service_worker`. |

## CSS Token Baseline

| Token | Value | Purpose |
|---|---|---|
| `--color-canvas` | `#FAF7F2` | Main beige-white canvas. |
| `--color-surface` | `#F2EDE5` | Card surface. |
| `--color-elevated` | `#EDE5D8` | Hover/selected/elevated layer. |
| `--color-ink` | `#2A2520` | Primary near-black brown text. |
| `--color-muted` | `#7B7167` | Secondary text. |
| `--radius-card` | `16px` | Bookmark/card container radius. |
| `--radius-tile` | `12px` | Small bookmark tile radius. |
| `--space-1` | `4px` | Base spacing unit. |
| `--space-2` | `8px` | Small spacing. |
| `--space-3` | `12px` | Medium spacing. |
| `--space-4` | `16px` | Default spacing. |
| `--space-6` | `24px` | Section spacing. |
| `--space-8` | `32px` | Large whitespace. |
| `--font-stack-ui` | `Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif` | UI font stack. |

## Source Layout

| Path | Role | Edit guidance |
|---|---|---|
| `manifest.json` | Extension metadata and permissions | Treat as high-risk. Prefer MV3 when migrating. |
| `background.js` | Background behavior | Preserve unless implementing explicit background/service-worker task. |
| `ntp/index.html` | New tab shell | Keep `#app` or equivalent mount target. |
| `ntp/ntp.css` | Main design system and new tab visual language | Mine and tokenise before deleting or replacing. |
| `ntp/ntp.js` | New tab runtime | Candidate for vanilla JS rewrite when implementing real bookmark rendering. |
| `popup/popup.*` | Toolbar popup | Empty/near-empty shell; safe to enable when scoped. |
| `options/options.*` | Options page | Empty/near-empty shell; safe to enable when scoped. |
| `icons/`, `images/`, `fonts/` | Static assets | Preserve paths unless manifest/UI references are updated. |

## Safe Modification Rules

| Change type | Rule |
|---|---|
| Aesthetic refinements | Start with CSS variables and class-level refactors. |
| Bookmark rendering | Use native browser bookmark APIs where possible. |
| New dependencies | Require explicit user approval. |
| Framework migration | Require explicit user approval. |
| MV3 migration | Allowed when scoped; verify Chrome and Firefox behavior separately. |
| Store metadata | Track permission justifications if Chrome Web Store submission becomes in-scope. |

## Validation Rules

| Validation | Command or action | Expected result |
|---|---|---|
| Manifest sanity | Load unpacked extension in Chrome | No manifest parse errors. |
| Firefox sanity | Load temporary add-on in Firefox | No manifest parse errors or unsupported API warnings that block load. |
| New tab UI | Open a new tab | Beige minimal card layout appears and no console errors are present. |
| Popup UI | Open toolbar action popup | Popup opens without layout overflow or console errors. |
| Options UI | Open options page if enabled | Options page opens without console errors. |
| Visual inspection | Use Chrome DevTools MCP when available | Screenshot/DOM/CSS inspection confirms high whitespace and card hierarchy. |
```

- [ ] **Step 3: Verify Qlearly guide contains root guide heading and specialization heading**

Run:

```powershell
Select-String -LiteralPath "D:\Aworker\crx\qlearly_bookmark-0.0.2\agent.md" -Pattern "Browser Extension Agent Guide|Qlearly Project Specialization"
```

Expected:

```text
D:\Aworker\crx\qlearly_bookmark-0.0.2\agent.md:1:# Browser Extension Agent Guide
D:\Aworker\crx\qlearly_bookmark-0.0.2\agent.md:<line>:# Qlearly Project Specialization
```

---

### Task 3: Final Consistency Review

**Files:**
- Read: `D:\Aworker\crx\agent.md`
- Read: `D:\Aworker\crx\qlearly_bookmark-0.0.2\agent.md`
- Read: `D:\Aworker\crx\docs\superpowers\specs\2026-07-08-agents-md-design.md`

**Interfaces:**
- Consumes: Files created in Tasks 1 and 2.
- Produces: Evidence that both files match the approved spec.

- [ ] **Step 1: Verify both files exist**

Run:

```powershell
Test-Path -LiteralPath "D:\Aworker\crx\agent.md"
Test-Path -LiteralPath "D:\Aworker\crx\qlearly_bookmark-0.0.2\agent.md"
```

Expected:

```text
True
True
```

- [ ] **Step 2: Verify Qlearly guide includes full root guide content**

Run:

```powershell
$root = Get-Content -LiteralPath "D:\Aworker\crx\agent.md" -Raw
$project = Get-Content -LiteralPath "D:\Aworker\crx\qlearly_bookmark-0.0.2\agent.md" -Raw
$project.StartsWith($root)
```

Expected:

```text
True
```

- [ ] **Step 3: Verify excluded broad tool inventories are absent**

Run:

```powershell
Select-String -LiteralPath "D:\Aworker\crx\agent.md","D:\Aworker\crx\qlearly_bookmark-0.0.2\agent.md" -Pattern "docx|pdf|pptx|aihot|khazix|maya|omniroute|exa|perplexity|codegraph|Full skm|Full opencode" -CaseSensitive:$false
```

Expected:

```text
(no output)
```

- [ ] **Step 4: Verify required CRX terms are present**

Run:

```powershell
Select-String -LiteralPath "D:\Aworker\crx\agent.md" -Pattern "chrome-extensions|modern-web-guidance|frontend-design|chrome-devtools-mcp|WXT|Manifest V3"
```

Expected: at least one match for each term.

- [ ] **Step 5: Verify Qlearly tokens are present**

Run:

```powershell
Select-String -LiteralPath "D:\Aworker\crx\qlearly_bookmark-0.0.2\agent.md" -Pattern "--color-canvas|--color-surface|--color-elevated|--color-ink|--radius-card|--font-stack-ui"
```

Expected: at least one match for each token.

- [ ] **Step 6: Report completion**

Expected report format:

```text
Created:
- D:\Aworker\crx\agent.md
- D:\Aworker\crx\qlearly_bookmark-0.0.2\agent.md

Verified:
- Project guide starts with root guide: True
- Excluded non-CRX tools absent: True
- Required CRX terms present: True
- Qlearly tokens present: True
```

## Plan Self-Review

| Check | Result |
|---|---|
| Spec coverage | Covered root guide, Qlearly guide, exclusions, allowed CRX tools, commands, traps, and token baseline. |
| Placeholder scan | No TBD/TODO/placeholders. |
| Type/path consistency | Uses exact Windows paths from spec. |
| Scope control | Does not implement Qlearly UI; only creates agent docs. |
| Dependency control | No npm/skm/opencode configuration changes. |
