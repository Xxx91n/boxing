# W920 Followup 实施呈报 — 2026-09-22

> 身份: 修复/开发子 Agent · 任务书: `.scratch/wave9-920-followup/handoffs/next-round.md`（D-010 切换，用户拍板「1」）
> 版本控制: WORKFLOW §4.2 唯一 `but` · 分支 `w920-followup-impl` · 已 push origin（未 tag / 未开 PR / 未代签 G-B / 未做 C08）

## 验收标准（用户原文）

> 编译通过、打包通过、启动并测活软件进程；每个平台都要有 test 闭环，避免只引入却没做到。

## M 步骤完成态

| Step | 内容 | 状态 | 证据 |
|---|---|---|---|
| M0 | 读任务书+账本+spec+plan，复述主序 | ✅ | plan-01..07 / M0–M8 |
| M1 | C05 addBookmark→commit + C07 分层豁免 | ✅ | commit `vsk`；guard self-test 19/19；live exemptions 9 全具名+expires+ticket |
| M2 | C01 三豁免处置（禁续期） | ✅ | commit `wlr`；empty-state/search=`closed`；zoom=`expired-handled`；active=0；waiver-check OK |
| M3 | C04 github-pages env v* 放行 | ✅ | commit `rks`；policy id **60663394**；reports/C04-environment-policy.md |
| M4 | G-A 四 job 全绿 | ✅ | run **35694278677** 4/4 success；reports/GA-run-35694278677.md |
| M5 | G-B 备包+检查单，**等用户声明** | ⏸ **停在此处** | artifacts/gb-2026.9.20/ + G-B-checklist；**待用户重签** |
| M6 | G-C | 未开始（绑 M5 后） | — |
| M7 | 文档账本收口 | 部分 | 本报告；release-notes 回填待 M6 后 |
| M8 | 三门汇报 + C08 交还用户 | 本报告末 | — |

## 验收对照（可复跑）

| 项 | 结果 | 命令 / 锚点 |
|---|---|---|
| 编译通过 | ✅ | `npm run build` → DONE_BUILD；manifest 2026.9.20 |
| 打包通过 | ✅ | `dist/boxing-chrome/release/chrome/boxing-2026.9.20.zip` 等 4 件；sha256 见 G-B-checklist |
| 启动并测活 | ✅ | Playwright 加载扩展跑 e2e（进程级）；bookmark chromium 23/23；empty+zoom 26/26 |
| 每平台 test 闭环 | ✅ | CI run 35694278677: ubuntu / windows / macos + data-golden **四 job 全绿** |
| 门禁 | ✅ | `npm run pretest` 全绿；`waiver-ledger-check` exit 0；layout-bypass 19/19 self-test |

## 变更摘要

1. **C05**: `ntp/render.js` 新增 `mutationHandlers.addBookmark`；`ntp/popups.js` add 路径改 `commit("addBookmark",…,{save:true})`（点击/Enter 统一 `addBmAction`）
2. **C07**: 9 条 `layout-bypass-allow` 全部补 `expires:2027-03-31; ticket:108`；guard 强制 expires+ticket（fail-closed）；删除类保持 0；计数 9→9 不升
3. **C01**: WORKFLOW 台账 3 条过期 active 按 D-003 处置（不续期、不退役已修用例）
4. **C04**: github-pages environment 新增 `v*` deployment branch policy

## commit 链（but / w920-followup-impl）

| id | 消息 |
|---|---|
| vsk | feat(w920): C05 addBookmark via commit(op) + C07 named expiring layout-bypass exemptions |
| wlr | docs(w920): C01 dispose expired flaky waivers |
| rks | docs(w920): C04 evidence — github-pages env allow v* |

## 缺项 / 边界（诚实声明）

| 项 | 状态 |
|---|---|
| G-A 字面 “main test.yml” | run 在 **w920-followup-impl** tip；land main 后可再跑一次 workflow_dispatch 坐实 |
| G-B | **未达成** — 等用户对 2026.9.20 zip 重签（版本+日期）；agent 不代签 |
| G-C | 未开始（M6）；C04 已为 tag deploy 放行 |
| C08 tag/商店 | **交还用户**（D-002） |
| zoom-dblclick 台账 | 仍 `expired-handled`（105 双绿不足）；非续期 |
| 三门合取 | **未齐** — 禁止宣称可发行 |

## 复跑命令

```bash
cd D:/Aworker/crx/boxing
npm run build && npm run pretest
node scripts/waiver-ledger-check.mjs
node scripts/layout-bypass-guard.mjs --self-test
npx playwright test --config=test/playwright.config.ts --project=chromium-extension --grep bookmark --reporter=line
gh run view 35694278677 --json conclusion,jobs
```

## M8 — 三门状态（交还）

| 门 | 状态 |
|---|---|
| **G-A** | ✅ run 35694278677 四 job 全绿 + active F=0（tip: w920-followup-impl） |
| **G-B** | ⏳ **等用户声明**（2026.9.20 · 日期）— 产物与检查单已备 |
| **G-C** | ⏳ 待 G-B 后执行 pages-gc-verify |
| **C08** | 👤 用户（tag/商店） |

**结论: 状态保持「不可发行」直至 G-B 重签 + G-C 过闸。**
