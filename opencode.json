{
  "$schema": "https://opencode.ai/config.json",
  "plugin": [
    "context-mode"
  ],
  "mcp": {
    "codegraph": {
      "type": "local",
      "command": [
        "codegraph",
        "serve",
        "--mcp"
      ],
      "enabled": true
    }
  },
  "rules": {
    "CRX": {
      "description": "Boxing browser extension development rules",
      "extends": "../agent.md"
    }
  }
}
